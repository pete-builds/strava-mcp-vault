x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  strava-mcp-vault:
    build: .
    image: strava-mcp-vault:0.2.0       # explicit version tag, NOT :latest
    container_name: strava-mcp-vault
    ports:
      - "${MCP_BIND_HOST:-0.0.0.0}:${STRAVA_MCP_PORT:-18201}:18201"
    env_file: .env
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - strava-data:/app/data
    logging: *default-logging
    healthcheck:
      test: ["CMD", "strava-mcp-vault-healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - strava-net

volumes:
  strava-data:

networks:
  strava-net:
